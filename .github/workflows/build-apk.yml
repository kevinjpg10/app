name: Build Android APK Release

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install dependencies
        working-directory: ./frontend 
        run: yarn install

      - name: Expo prebuild
        working-directory: ./frontend
        # Inyectamos las variables AQUÍ para que se guarden dentro del APK
        env:
          EXPO_PUBLIC_GEMINI_API_KEY: ${{ secrets.EXPO_PUBLIC_GEMINI_API_KEY }}
          EXPO_PUBLIC_BACKEND_URL: ${{ secrets.EXPO_PUBLIC_BACKEND_URL }}
        run: npx expo prebuild --platform android --no-install

      - name: Generate Keystore
        run: |
          keytool -genkeypair \
            -v \
            -keystore efrain-release.keystore \
            -alias efrain-key \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -storepass efrain1234 \
            -keypass efrain1234 \
            -dname "CN=Efrain Gastos, OU=App, O=Efrain, L=Madrid, S=Madrid, C=ES"

      - name: Copy keystore to android folder
        run: cp efrain-release.keystore frontend/android/app/efrain-release.keystore

      - name: Configure signing in gradle
        working-directory: ./frontend/android
        run: |
          cat >> app/build.gradle << 'GRADLE'
          android {
            signingConfigs {
              release {
                storeFile file("efrain-release.keystore")
                storePassword "efrain1234"
                keyAlias "efrain-key"
                keyPassword "efrain1234"
              }
            }
            buildTypes {
              release {
                signingConfig signingConfigs.release
              }
            }
          }
          GRADLE

      - name: Build Release APK
        working-directory: ./frontend/android
        # Volvemos a pasar las variables por si acaso durante la compilación final
        env:
          EXPO_PUBLIC_GEMINI_API_KEY: ${{ secrets.EXPO_PUBLIC_GEMINI_API_KEY }}
          EXPO_PUBLIC_BACKEND_URL: ${{ secrets.EXPO_PUBLIC_BACKEND_URL }}
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: efrain-gastos-apk
          path: frontend/android/app/build/outputs/apk/release/app-release.apk
